{
  "name": "chat-pgs",
  "version": "0.0.0",
  "type": "module",
  "files":["dist"],
  "module": "dist/chat-pgs.js",
  "exports": {
    ".": {
      "import": "./dist/chat-pgs.js"
    }
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build --config vite-app.config.js && vite build --config vite-lib.config.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^4.0.0"
  }
}
